import { afterEach, describe, expect, it, vi } from 'vitest'; import { ApiClient } from '../../core/api/client'
describe('ApiClient',()=>{ afterEach(()=>vi.unstubAllGlobals()); it('performs one refresh for concurrent 401 responses',async()=>{let calls=0, requests=0; vi.stubGlobal('fetch',vi.fn().mockImplementation(async()=>++requests<=2?new Response('',{status:401}):new Response(JSON.stringify({data:{ok:true}}),{status:200}))); const client=new ApiClient('',()=> 'old',async()=>{calls++;await Promise.resolve();return 'new'}); await Promise.all([client.get('/ledgers'),client.get('/auth/devices')]); expect(calls).toBe(1)}) })
